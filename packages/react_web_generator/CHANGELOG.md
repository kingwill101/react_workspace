## 0.1.0

- Introduce the Web IDL generation pipeline for portable React DOM bindings,
  browser adapters, SSR metadata, host factories, and completeness reports.
- Pin generation to the published `package:web` 1.1.1 surface.
- Separate browser-adapter planning from source rendering and add deterministic
  generation and output-parity tests.
