name: react_testing
resolution: workspace
description: Test harnesses for React Dart client, SSR, and server functions.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  react_server: {path: ../react_server}
  react_tool: {path: ../react_tool}
  server_testing: 0.4.0
  server_testing_shelf: 0.4.0
  shelf: ^1.4.0
  shelf_static: ^1.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.2
