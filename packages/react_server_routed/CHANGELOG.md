# Changelog

## 0.1.1

- Widens the supported `routed_core` range to releases before 1.0.0.

## 0.1.0

- Introduces Routed handlers for React documents, assets, SSR, and server actions.
- Keeps Routed dependencies outside the transport-neutral `react_server` package.
