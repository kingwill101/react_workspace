/// The complete, neutral Web platform surface generated from the pinned
/// `dart-lang/web` snapshot.
///
/// Every definition and member in the snapshot is present here; SSR operations
/// that are not executable on the server throw rather than being omitted.
///
/// ```dart
/// import 'package:react_web/web.dart';
///
/// final storage = window.localStorage;
/// storage.setItem('theme', 'dark');
/// ```
library;

export 'src/generated/web/web.dart';
export 'src/generated/web/globals.dart';
export 'src/web_runtime.dart';
