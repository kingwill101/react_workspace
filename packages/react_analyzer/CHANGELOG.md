## 0.1.0

- Introduces live React Dart diagnostics for components, hooks, server rendering, and import boundaries.
- Adds quick fixes for client-only APIs and generated bridge imports, plus a component conversion assist.
