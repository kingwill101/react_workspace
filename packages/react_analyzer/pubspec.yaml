name: react_analyzer
description: Analyzer plugin for React Dart — live diagnostics, fixes, and assists.
version: 0.1.0
repository: https://github.com/kingwill101/react_workspace/tree/master/packages/react_analyzer
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  analysis_server_plugin: ^0.3.0
  analyzer: ">=7.0.0 <16.0.0"
  analyzer_plugin: ">=0.13.0 <1.0.0"
  react_analysis:
    path: ../react_analysis

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.2
