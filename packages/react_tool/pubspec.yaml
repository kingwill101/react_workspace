name: react_tool
resolution: workspace
description: CLI for building and running React Dart applications.
version: 0.1.0

executables:
  react: react

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  artisanal: ^0.5.0
  package_config: ^3.0.0
  path: ^1.9.0
  sass: ^1.102.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.2
