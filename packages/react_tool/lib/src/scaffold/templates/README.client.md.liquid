# {{ title }}

A React Dart application (client-only, no SSR).

## Setup

```sh
dart pub get
```

## Build

```sh
dart run react_tool:react build
```

## Run

```sh
dart run react_tool:react serve
```

For day-to-day iteration, focus on `lib/react/app.dart`.
Generated output is in `lib/.generated/` and `build/`.

VS Code users get `.vscode/settings.json` preconfigured to hide generated and
build artifacts (`.generated`, `build`, `.dart_tool`) from the Explorer by
default.

## Test

```sh
dart test
dart run react_tool:react test --coverage
```

Tests use `react_testing` (`ReactComponentHarness`, `InMemorySsrHarness`)
— see `test/app_test.dart`. The client-only scaffold does not include a
server-function file or server-function tests.

## Analyze

```sh
dart run react_tool:react analyze
```
```

The server runs on `http://localhost:8080` and serves the client bundle statically.
