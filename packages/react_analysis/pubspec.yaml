name: react_analysis
description: Shared analyzer engine for React Dart — component, hook, SSR, and runtime-usage semantics.
version: 0.1.1
repository: https://github.com/kingwill101/react_workspace/tree/master/packages/react_analysis
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  analyzer: ">=7.0.0 <16.0.0"

dev_dependencies:
  test: ^1.31.2
