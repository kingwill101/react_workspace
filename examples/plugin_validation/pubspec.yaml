name: plugin_validation
description: Validates react_analyzer plugin diagnostics, fixes, and runtime usage.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  react_core: ^0.1.0
  react_analysis: ^0.1.0
  react_js: ^0.1.0
  react_web: ^0.1.0
  web: ^1.1.1

dev_dependencies:
  analyzer: ">=7.0.0 <16.0.0"
  build_runner: ^2.15.3
  lints: ^6.0.0
  react_analyzer: ^0.1.0
  react_codegen: ^0.1.0
  test: ^1.31.2
